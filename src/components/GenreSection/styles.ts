import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  genreContainer: {
    marginBottom: 20,
  },

  genreTitle: {
    fontSize: 20,
    fontWeight: "bold",
    color: "#fff",
    marginLeft: 10,
    marginBottom: 10,
  },

  moviesList: {
    paddingHorizontal: 5,
  },
});

export default styles;
